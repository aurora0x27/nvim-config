--------------------------------------------------------------------------------
-- WARN: This file is generated by script, DO NOT EDIT!
--------------------------------------------------------------------------------
-- luacheck: ignore 631 -- max line length
---@alias DashboardArtName
---|'DoomNvim'
---|'Ayanami Rei'
---|'Vscode'
---|'Saturn'
---|'Saiba Momoi'
---|'Lenin'
---|'Default'

---@alias DiagnoseLevel
---|'hint'
---|'info'
---|'warn'
---|'error'

---@alias DiagnoseMode
---|'inline'
---|'pretty'
---|'detailed'

---@alias StatusScrollbarStyle
---|'sbar'
---|'circle'
---|'moon'

---@class Profile
---@field allow_workspace_patch boolean allow editor patch its behavior according to workspace config
---@field bigfile_size_byte number average byte size
---@field bigfile_size_line number average line length (useful for minified files)
---@field blink_use_binary boolean blink.cmp use prebuild binary instead of building
---@field clang_format_path string assign `clang-format` binary path
---@field clangd_path string assign `clangd` binary path
---@field dashboard_art_name DashboardArtName choose an ascii art on dashboard
---@field diagnose_level DiagnoseLevel minimal level of diagnostic messages to display
---@field diagnose_mode DiagnoseMode diagnose display level.
---       options are `'inline'|'detailed'|'pretty'`, `inline` means use virtual text
---       to display diagnostic messages, `detailed` means use extra virtual lines,
---       `pretty` means use extra plugin -- `tiny-inline-diagnostic` to display
---@field diagnose_with_fancy_underline boolean use fancy curl underline. **need terminal and tmux support**
---@field disable_im_switch boolean disable auto im switcher
---@field emmy_inject_plugin_path boolean inject plugin path to `emmylua_ls` workspace config
---@field emmy_inject_vim_rt boolean inject vim runtime to `emmylua_ls` workspace config
---@field enable_current_line_blame boolean enable virtual text line blame at the end of line
---@field enable_inlay_hint boolean default enable lsp inlay hint if has capability
---@field enable_relative_lnum boolean use relative number
---@field enable_sticky_buffer boolean enable sticky buffer for each window
---@field integrated_terminal_shell string choose integrated terminal shell, default zsh
---@field lang_blacklist string disabled lang configs, default none, split by ','
---@field lang_levels string lang feature config.
---       syntax: string `c:full;cpp:none;rust:lsp,+ts,-fmt` means enable full
---       feature for c, disable all features for cpp, enable tree-sitter and lsp,
---       disable formatter for rust.
---@field lang_whitelist string enabled lang configs, default all, split by ','
---@field persist_local_dir string Workspace-relative directory used to store local persistence data.
---@field persist_local_mode string Store selected persistence features in the current workspace. Supported items: `session|undo|shada|swap`.
---@field persist_mode string Enable persistence features. Supported items: `session|undo|shada|swap`
---@field statline_scrollbar_style StatusScrollbarStyle heirline scroll bar style, which displays cursor position
---@field transparent_mode boolean enable transparent mode
---@field use_emmylua_ls boolean use `emmylua_ls` as lua language server
---@field use_neogit boolean use [`neogit`](https://github.com/neogitorg/neogit) as enhanced git client
---@field use_ufo_as_fold_provider boolean use [`nvim-ufo`](https://github.com/kevinhwang91/nvim-ufo) as fold provider to get better code fold experience
---@field workspace_patch_always_restrict boolean always enable restrict mode, disable _dofile_ to prevent **ACE**
